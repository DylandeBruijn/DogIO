// Import React
import React from "react";

// Import Styled Components
import styled from "styled-components";

export const DogFilter = () => {
	return (
		<section>
			<h2>Dog Filter Component</h2>
		</section>
	);
};
